'use strict';
/**
 * Sample transaction
 * @param {org.dfs.foo} foo
 * @transaction
 */
async function foo(transaction) {
    // var dataOwner = transaction.data.owner;
    // var participant = getCurrentParticipant();
    // if (dataOwner.id !== participant.id) {
    //     throw new Error("Participant not authorized");
    // }
    // emit(getFactory().newEvent('org.dfs', 'DataAccessResponse'));
    return;
}